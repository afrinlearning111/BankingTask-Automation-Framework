name: BankingTask - Maven CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      # Step 1: Checkout repository
      - name: Checkout Code
        uses: actions/checkout@v3

      # Step 2: Set up Java 21
      - name: Setup JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      # Step 3: Cache Maven Dependencies
      - name: Cache Maven packages
        uses: actions/cache@v3
        with:
          path: |
            ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      # Step 4: Install Google Chrome
      - name: Install Chrome
        run: choco install googlechrome --no-progress

      # Step 5: Install ChromeDriver
      - name: Install ChromeDriver
        run: choco install chromedriver --ignore-checksums --no-progress

      # Step 6: Run Maven Tests
      - name: Run Tests (Maven)
        run: mvn clean test -X

      # Step 7: Upload Extent Reports
      - name: Upload Extent Report Artifacts
        uses: actions/upload-artifact@v3
        with:
          name: ExtentReports
          path: extent-reports/
          if-no-files-found: warn

      # Step 8: Upload TestNG Reports
      - name: Upload TestNG Reports
        uses: actions/upload-artifact@v3
        with:
          name: TestNG-Reports
          path: test-output/
          if-no-files-found: warn
